{
    "version": "0.2.0",
    "configurations": [
        
        {
            "type": "pwa-node",
            "request": "launch",
            "name": "Launch Deno",
            "program": "${workspaceFolder}/x.ts",
            "cwd": "${workspaceFolder}",
            "runtimeExecutable": "denon",
            "runtimeArgs": [
                "run",
                "--allow-all",
                "--unstable"
            ]
        },

    ]
}